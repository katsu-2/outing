require 'rails_helper'

RSpec.describe Folder, type: :model do
  # describe "バリデーションテスト" do
  #   it "名前が無ければ無効" do
  #     folder = create(:folder, name: nil)
  #     folder.valid?
  #     expect(folder.errors[:name]).to include("が入力されていません。")
  #   end
  # end
end
